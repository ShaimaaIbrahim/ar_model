name: my_custom_lint_rules
description: قواعد مخصصة لفرض معايير Clean Architecture
version: 0.0.1
publish_to: none # لمنع رفعها للعموم بالخطأ

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  analyzer: any
  custom_lint_builder: ^0.5.0